# Generated branch (gh-pages)

This branch contains **generated output only** for GitHub Pages.

- Source code and configuration live on the `main` branch.
- Do not edit files here by hand — changes will be overwritten by automation.

## What’s in this branch
- `index.html` (route index)
- `*.html` (one page per route)
- `.run-state/last_success.json` (run-once-per-day marker)

## Where to make changes
Edit `routes.txt` or `generate.py` on `main`, then run the workflow (or wait for the scheduled run).
